---
import katex from 'katex';

interface Props {
  tex: string;
}

const { tex } = Astro.props;

const html = katex.renderToString(tex, {
  displayMode: true,
  throwOnError: false,
  output: 'html',
});
---

<div class="katex-block my-6 overflow-x-auto" set:html={html} />
