/**
 * Modelica source for fixed-wing plant-only model (no controller).
 * Used in Autopilot mode -- control surfaces come from external autopilot.
 * Same as the controller model but inputs are driven by the autopilot.
 */
export { FIXEDWING_CONTROLLER_MODEL as FIXEDWING_PLANT_MODEL } from './fixedwing-controller-model';
export { FIXEDWING_MODEL_NAME as FIXEDWING_PLANT_MODEL_NAME } from './fixedwing-controller-model';
