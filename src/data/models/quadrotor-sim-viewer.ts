export const QUADROTOR_SIM_VIEWER_SCRIPT = "// High-fidelity quadrotor visualization for AutopilotSim results.\n// NED frame mapping: px(North)\u2192Z, py(East)\u2192X, pz(Down)\u2192-Y in THREE.js (Y-up).\n\nconst ARM_LEN = 0.28;\nconst TRAIL_MAX = 800;\n\nctx.onInit = (api) => {\n  }\n  const { THREE, state } = api;\n\n  // \u2550\u2550\u2550 DESERT SKY \u2550\u2550\u2550\n  // Gradient sky using a large sphere with vertex colors\n  const skyGeo = new THREE.SphereGeometry(200, 32, 16);\n  const skyColors = [];\n  const posAttr = skyGeo.getAttribute(\"position\");\n  for (let i = 0; i < posAttr.count; i++) {\n    const y = posAttr.getY(i);\n    const t = Math.max(0, Math.min(1, (y / 200 + 1) * 0.5)); // 0=bottom, 1=top\n    // Horizon: warm haze (0xf0d8b0), zenith: clear blue (0x4a90c8)\n    const r = 0.94 + (0.29 - 0.94) * Math.pow(t, 0.6);\n    const g = 0.85 + (0.56 - 0.85) * Math.pow(t, 0.6);\n    const b = 0.69 + (0.78 - 0.69) * Math.pow(t, 0.6);\n    skyColors.push(r, g, b);\n  }\n  skyGeo.setAttribute(\"color\", new THREE.Float32BufferAttribute(skyColors, 3));\n  const skyMat = new THREE.MeshBasicMaterial({ vertexColors: true, side: THREE.BackSide });\n  const sky = new THREE.Mesh(skyGeo, skyMat);\n  state.scene.add(sky);\n  state.scene.background = null; // let sky sphere handle it\n\n  // \u2550\u2550\u2550 DESERT LIGHTING \u2014 warm sun \u2550\u2550\u2550\n  const sun = new THREE.DirectionalLight(0xfff0d0, 1.8);\n  sun.position.set(8, 12, 4);\n  sun.castShadow = true;\n  state.scene.add(sun);\n  const fill = new THREE.DirectionalLight(0xd4a060, 0.4);\n  fill.position.set(-5, 3, -4);\n  state.scene.add(fill);\n  const rim = new THREE.DirectionalLight(0xffeebb, 0.25);\n  rim.position.set(0, -1, -6);\n  state.scene.add(rim);\n  state.scene.add(new THREE.HemisphereLight(0x87ceeb, 0xc2956b, 0.5));\n\n  // \u2550\u2550\u2550 DESERT GROUND \u2550\u2550\u2550\n  // Sandy floor\n  const sandMat = new THREE.MeshStandardMaterial({\n    color: 0xd4a860, roughness: 0.95, metalness: 0.02\n  });\n  const floor = new THREE.Mesh(new THREE.PlaneGeometry(200, 200), sandMat);\n  floor.rotation.x = -Math.PI / 2;\n  floor.position.y = -0.01;\n  floor.receiveShadow = true;\n  state.scene.add(floor);\n\n  // Subtle grid for spatial reference (faint lines in sand)\n  const grid = new THREE.GridHelper(50, 50, 0xc49850, 0xc49850);\n  grid.material.transparent = true;\n  grid.material.opacity = 0.12;\n  grid.position.y = 0.0;\n  state.scene.add(grid);\n\n  // \u2550\u2550\u2550 SAND DUNES \u2550\u2550\u2550\n  const duneMat = new THREE.MeshStandardMaterial({\n    color: 0xd9b06a, roughness: 0.9, metalness: 0.0\n  });\n  const duneDarkMat = new THREE.MeshStandardMaterial({\n    color: 0xc49850, roughness: 0.95, metalness: 0.0\n  });\n  // Place dunes around the periphery\n  const duneConfigs = [\n    { x: -18, z: 20, sx: 12, sy: 1.5, sz: 5, ry: 0.3 },\n    { x: 22, z: 15, sx: 15, sy: 2.0, sz: 6, ry: -0.2 },\n    { x: -25, z: -10, sx: 10, sy: 1.2, sz: 4, ry: 0.5 },\n    { x: 15, z: -22, sx: 18, sy: 2.5, sz: 7, ry: 0.1 },\n    { x: -10, z: -25, sx: 14, sy: 1.8, sz: 5, ry: -0.4 },\n    { x: 30, z: -5, sx: 8, sy: 1.0, sz: 4, ry: 0.6 },\n    { x: -30, z: 5, sx: 11, sy: 1.3, sz: 5, ry: -0.1 },\n    { x: 0, z: 30, sx: 20, sy: 2.2, sz: 6, ry: 0.15 },\n  ];\n  duneConfigs.forEach((d) => {\n    const dune = new THREE.Mesh(\n      new THREE.SphereGeometry(1, 16, 8, 0, Math.PI * 2, 0, Math.PI / 2),\n      Math.random() > 0.5 ? duneMat : duneDarkMat\n    );\n    dune.scale.set(d.sx, d.sy, d.sz);\n    dune.position.set(d.x, -0.01, d.z);\n    dune.rotation.y = d.ry;\n    state.scene.add(dune);\n  });\n\n  // \u2550\u2550\u2550 DESERT ROCKS \u2550\u2550\u2550\n  const rockMat = new THREE.MeshStandardMaterial({\n    color: 0x8b7355, roughness: 0.85, metalness: 0.05\n  });\n  const rockDarkMat = new THREE.MeshStandardMaterial({\n    color: 0x6b5740, roughness: 0.9, metalness: 0.05\n  });\n  const rockConfigs = [\n    { x: 5, z: 8, s: 0.3 }, { x: -7, z: 6, s: 0.5 },\n    { x: 9, z: -4, s: 0.2 }, { x: -4, z: -8, s: 0.4 },\n    { x: 12, z: 3, s: 0.35 }, { x: -11, z: -3, s: 0.25 },\n    { x: 3, z: -10, s: 0.45 }, { x: -8, z: 10, s: 0.3 },\n  ];\n  rockConfigs.forEach((r) => {\n    const rock = new THREE.Mesh(\n      new THREE.DodecahedronGeometry(r.s, 1),\n      Math.random() > 0.5 ? rockMat : rockDarkMat\n    );\n    rock.position.set(r.x, r.s * 0.3, r.z);\n    rock.scale.set(1 + Math.random() * 0.5, 0.5 + Math.random() * 0.4, 1 + Math.random() * 0.3);\n    rock.rotation.set(Math.random() * 0.3, Math.random() * Math.PI, Math.random() * 0.2);\n    state.scene.add(rock);\n  });\n\n  // \u2550\u2550\u2550 CACTI \u2550\u2550\u2550\n  const cactusMat = new THREE.MeshStandardMaterial({\n    color: 0x3a6b35, roughness: 0.8, metalness: 0.05\n  });\n  const cactusConfigs = [\n    { x: -6, z: 12 }, { x: 14, z: 7 }, { x: -13, z: -6 },\n    { x: 8, z: -12 }, { x: -3, z: -15 },\n  ];\n  cactusConfigs.forEach((c) => {\n    const height = 0.8 + Math.random() * 1.2;\n    // Main trunk\n    const trunk = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.12, 0.15, height, 8),\n      cactusMat\n    );\n    trunk.position.set(c.x, height / 2, c.z);\n    state.scene.add(trunk);\n    // Rounded top\n    const top = new THREE.Mesh(new THREE.SphereGeometry(0.12, 8, 6), cactusMat);\n    top.position.set(c.x, height, c.z);\n    state.scene.add(top);\n    // Side arm (randomly on some)\n    if (Math.random() > 0.3) {\n      const armH = 0.4 + Math.random() * 0.4;\n      const armY = height * 0.4 + Math.random() * height * 0.3;\n      const armDir = Math.random() > 0.5 ? 1 : -1;\n      // Horizontal segment\n      const armHoriz = new THREE.Mesh(\n        new THREE.CylinderGeometry(0.07, 0.08, 0.3, 6),\n        cactusMat\n      );\n      armHoriz.rotation.z = armDir * Math.PI / 2;\n      armHoriz.position.set(c.x + armDir * 0.2, armY, c.z);\n      state.scene.add(armHoriz);\n      // Vertical segment going up\n      const armVert = new THREE.Mesh(\n        new THREE.CylinderGeometry(0.06, 0.07, armH, 6),\n        cactusMat\n      );\n      armVert.position.set(c.x + armDir * 0.35, armY + armH / 2, c.z);\n      state.scene.add(armVert);\n      const armTop = new THREE.Mesh(new THREE.SphereGeometry(0.06, 6, 4), cactusMat);\n      armTop.position.set(c.x + armDir * 0.35, armY + armH, c.z);\n      state.scene.add(armTop);\n    }\n  });\n\n  // \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n  //  BUILD HIGH-FIDELITY QUADROTOR\n  // \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n  const quad = new THREE.Group();\n  quad.name = \"quadrotor\";\n\n  // --- Material palette ---\n  const matBody = new THREE.MeshStandardMaterial({\n    color: 0x2a2a2a, metalness: 0.4, roughness: 0.45\n  });\n  const matBodyAccent = new THREE.MeshStandardMaterial({\n    color: 0x1a1a1e, metalness: 0.5, roughness: 0.3\n  });\n  const matCarbon = new THREE.MeshStandardMaterial({\n    color: 0x222222, metalness: 0.3, roughness: 0.6\n  });\n  const matMotor = new THREE.MeshStandardMaterial({\n    color: 0x111111, metalness: 0.85, roughness: 0.15\n  });\n  const matMotorBell = new THREE.MeshStandardMaterial({\n    color: 0x333338, metalness: 0.9, roughness: 0.1\n  });\n  const matLens = new THREE.MeshStandardMaterial({\n    color: 0x111122, metalness: 0.9, roughness: 0.05\n  });\n  const matGimbal = new THREE.MeshStandardMaterial({\n    color: 0xaaaaaa, metalness: 0.7, roughness: 0.2\n  });\n  const matBattery = new THREE.MeshStandardMaterial({\n    color: 0x2a2a30, metalness: 0.3, roughness: 0.6\n  });\n  const matGuard = new THREE.MeshStandardMaterial({\n    color: 0x333333, metalness: 0.2, roughness: 0.7, transparent: true, opacity: 0.6\n  });\n  const matSkid = new THREE.MeshStandardMaterial({\n    color: 0x444444, metalness: 0.3, roughness: 0.5\n  });\n\n  // \u2500\u2500 CENTRAL FUSELAGE \u2500\u2500\n  // Main body: rounded flattened shape (top shell + bottom shell)\n  const bodyTopGeo = new THREE.SphereGeometry(0.09, 24, 12, 0, Math.PI * 2, 0, Math.PI / 2);\n  const bodyTop = new THREE.Mesh(bodyTopGeo, matBody);\n  bodyTop.scale.set(1.3, 0.5, 1.0);\n  bodyTop.position.y = 0.005;\n  quad.add(bodyTop);\n\n  const bodyBotGeo = new THREE.SphereGeometry(0.09, 24, 12, 0, Math.PI * 2, Math.PI / 2, Math.PI / 2);\n  const bodyBot = new THREE.Mesh(bodyBotGeo, matBodyAccent);\n  bodyBot.scale.set(1.3, 0.35, 1.0);\n  bodyBot.position.y = 0.005;\n  quad.add(bodyBot);\n\n  // Body side panel detail \u2014 thin strips along the fuselage\n  for (const sz of [-1, 1]) {\n    const strip = new THREE.Mesh(\n      new THREE.BoxGeometry(0.22, 0.012, 0.004),\n      matCarbon\n    );\n    strip.position.set(0, 0.005, sz * 0.085);\n    quad.add(strip);\n  }\n\n  // Top cover plate\n  const coverGeo = new THREE.CylinderGeometry(0.065, 0.07, 0.008, 20);\n  const cover = new THREE.Mesh(coverGeo, matBodyAccent);\n  cover.position.y = 0.05;\n  quad.add(cover);\n\n  // \u2500\u2500 GPS / ANTENNA MAST \u2500\u2500\n  const antBase = new THREE.Mesh(\n    new THREE.CylinderGeometry(0.012, 0.014, 0.015, 8),\n    matCarbon\n  );\n  antBase.position.set(0, 0.058, -0.02);\n  quad.add(antBase);\n  const antMast = new THREE.Mesh(\n    new THREE.CylinderGeometry(0.003, 0.003, 0.035, 6),\n    matGimbal\n  );\n  antMast.position.set(0, 0.082, -0.02);\n  quad.add(antMast);\n  const antTop = new THREE.Mesh(\n    new THREE.SphereGeometry(0.006, 8, 6),\n    matGimbal\n  );\n  antTop.position.set(0, 0.1, -0.02);\n  quad.add(antTop);\n\n  // \u2500\u2500 BATTERY PACK (top-mounted) \u2500\u2500\n  const batt = new THREE.Mesh(\n    new THREE.BoxGeometry(0.08, 0.022, 0.045),\n    matBattery\n  );\n  batt.position.set(0, 0.04, 0.01);\n  quad.add(batt);\n  // Battery label stripe\n  const battStripe = new THREE.Mesh(\n    new THREE.BoxGeometry(0.04, 0.023, 0.002),\n    new THREE.MeshStandardMaterial({ color: 0xddaa00, metalness: 0.3, roughness: 0.5 })\n  );\n  battStripe.position.set(0, 0.04, 0.034);\n  quad.add(battStripe);\n\n  // \u2500\u2500 CAMERA GIMBAL \u2500\u2500\n  const gimbalMount = new THREE.Mesh(\n    new THREE.BoxGeometry(0.03, 0.012, 0.03),\n    matGimbal\n  );\n  gimbalMount.position.set(0, -0.025, 0.065);\n  quad.add(gimbalMount);\n\n  // Gimbal yoke\n  const gimbalYoke = new THREE.Mesh(\n    new THREE.BoxGeometry(0.025, 0.02, 0.025),\n    matGimbal\n  );\n  gimbalYoke.position.set(0, -0.038, 0.065);\n  quad.add(gimbalYoke);\n\n  // Camera housing\n  const camHousing = new THREE.Mesh(\n    new THREE.BoxGeometry(0.028, 0.018, 0.022),\n    matBody\n  );\n  camHousing.position.set(0, -0.038, 0.065);\n  quad.add(camHousing);\n\n  // Camera lens\n  const lens = new THREE.Mesh(\n    new THREE.CylinderGeometry(0.007, 0.008, 0.008, 12),\n    matLens\n  );\n  lens.rotation.x = Math.PI / 2;\n  lens.position.set(0, -0.038, 0.078);\n  quad.add(lens);\n\n  // Lens glass (reflective front element)\n  const lensGlass = new THREE.Mesh(\n    new THREE.CircleGeometry(0.006, 12),\n    new THREE.MeshStandardMaterial({ color: 0x223355, metalness: 1.0, roughness: 0.0 })\n  );\n  lensGlass.position.set(0, -0.038, 0.0825);\n  quad.add(lensGlass);\n\n  // \u2500\u2500 ARMS + MOTORS + PROPELLERS \u2500\u2500\n  // X-config: front-left, front-right, rear-left, rear-right\n  const armConfigs = [\n    { angle: Math.PI / 4,       label: \"FL\", cw: false, ledColor: 0x00ff44 },\n    { angle: -Math.PI / 4,      label: \"FR\", cw: true,  ledColor: 0xff2200 },\n    { angle: 3 * Math.PI / 4,   label: \"RL\", cw: true,  ledColor: 0xffffff },\n    { angle: -3 * Math.PI / 4,  label: \"RR\", cw: false, ledColor: 0xffffff },\n  ];\n\n  state.propGroups = [];\n\n  armConfigs.forEach((cfg) => {\n    const dx = Math.sin(cfg.angle) * ARM_LEN;\n    const dz = Math.cos(cfg.angle) * ARM_LEN;\n\n    // \u2500 ARM: tapered carbon tube \u2500\n    const armGeo = new THREE.CylinderGeometry(0.009, 0.012, ARM_LEN, 8);\n    const arm = new THREE.Mesh(armGeo, matCarbon);\n    // Rotate cylinder to lie flat and point toward motor position\n    arm.rotation.z = Math.PI / 2;\n    arm.rotation.y = -cfg.angle + Math.PI / 2;\n    arm.position.set(dx / 2, 0.005, dz / 2);\n    quad.add(arm);\n\n    // Arm reinforcement rib\n    const rib = new THREE.Mesh(\n      new THREE.BoxGeometry(ARM_LEN * 0.7, 0.018, 0.004),\n      matCarbon\n    );\n    rib.rotation.y = -cfg.angle + Math.PI / 2;\n    // Position rib along arm direction\n    const ribDist = 0.55;\n    rib.position.set(dx * ribDist, 0.005, dz * ribDist);\n    quad.add(rib);\n\n    // \u2500 MOTOR BASE (mount plate) \u2500\n    const motorBase = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.022, 0.024, 0.008, 12),\n      matCarbon\n    );\n    motorBase.position.set(dx, 0.0, dz);\n    quad.add(motorBase);\n\n    // \u2500 MOTOR STATOR (inner cylinder) \u2500\n    const stator = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.014, 0.014, 0.018, 10),\n      matMotor\n    );\n    stator.position.set(dx, 0.013, dz);\n    quad.add(stator);\n\n    // \u2500 MOTOR BELL (outer rotating shell) \u2500\n    // Bell top cap\n    const bellTop = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.018, 0.020, 0.012, 12),\n      matMotorBell\n    );\n    bellTop.position.set(dx, 0.024, dz);\n    quad.add(bellTop);\n\n    // Bell lip ring\n    const bellRing = new THREE.Mesh(\n      new THREE.TorusGeometry(0.019, 0.002, 6, 16),\n      matMotorBell\n    );\n    bellRing.rotation.x = Math.PI / 2;\n    bellRing.position.set(dx, 0.018, dz);\n    quad.add(bellRing);\n\n    // Motor shaft nub\n    const shaft = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.003, 0.003, 0.01, 6),\n      matMotor\n    );\n    shaft.position.set(dx, 0.035, dz);\n    quad.add(shaft);\n\n    // \u2500 PROPELLER (actual blade shapes) \u2500\n    const propGroup = new THREE.Group();\n    propGroup.position.set(dx, 0.038, dz);\n\n    // Prop hub\n    const hub = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.008, 0.008, 0.005, 8),\n      matCarbon\n    );\n    propGroup.add(hub);\n\n    // Two blades with airfoil-like cross section\n    for (let b = 0; b < 2; b++) {\n      const bladeAngle = b * Math.PI;\n      const blade = createPropBlade(THREE, cfg.cw);\n      blade.rotation.y = bladeAngle;\n      propGroup.add(blade);\n    }\n\n    quad.add(propGroup);\n    state.propGroups.push({ group: propGroup, cw: cfg.cw });\n\n    // \u2500 PROP GUARD (protective ring) \u2500\n    const guardGeo = new THREE.TorusGeometry(0.13, 0.004, 6, 32);\n    const guard = new THREE.Mesh(guardGeo, matGuard);\n    guard.rotation.x = Math.PI / 2;\n    guard.position.set(dx, 0.032, dz);\n    quad.add(guard);\n\n    // Guard support struts (2 per guard, connecting to arm)\n    for (const strutAngle of [cfg.angle - 0.3, cfg.angle + 0.3]) {\n      const sx = Math.sin(strutAngle) * 0.12;\n      const sz = Math.cos(strutAngle) * 0.12;\n      const strutLen = 0.04;\n      const strut = new THREE.Mesh(\n        new THREE.CylinderGeometry(0.002, 0.002, strutLen, 4),\n        matCarbon\n      );\n      strut.position.set(dx + sx * 0.3, 0.025, dz + sz * 0.3);\n      // Tilt strut outward\n      strut.rotation.z = (sx > 0 ? -1 : 1) * 0.4;\n      strut.rotation.x = (sz > 0 ? 1 : -1) * 0.4;\n      quad.add(strut);\n    }\n\n    // \u2500 NAVIGATION LED \u2500\n    const led = new THREE.Mesh(\n      new THREE.SphereGeometry(0.005, 8, 6),\n      new THREE.MeshStandardMaterial({\n        color: cfg.ledColor, emissive: cfg.ledColor, emissiveIntensity: 2.0\n      })\n    );\n    // Place LED on underside of arm near motor\n    led.position.set(dx * 0.85, -0.015, dz * 0.85);\n    quad.add(led);\n\n    // LED point light for glow\n    const ledLight = new THREE.PointLight(cfg.ledColor, 0.15, 0.3);\n    ledLight.position.copy(led.position);\n    quad.add(ledLight);\n  });\n\n  // \u2500\u2500 LANDING GEAR \u2500\u2500\n  // Two skid rails (front-to-back) with 4 vertical struts\n  for (const side of [-1, 1]) {\n    const skidZ = side * 0.055;\n\n    // Main skid rail\n    const rail = new THREE.Mesh(\n      new THREE.CapsuleGeometry(0.005, 0.22, 4, 8),\n      matSkid\n    );\n    rail.rotation.z = Math.PI / 2;\n    rail.position.set(0, -0.065, skidZ);\n    quad.add(rail);\n\n    // Vertical struts connecting body to skids\n    for (const fb of [-0.06, 0.06]) {\n      const strut = new THREE.Mesh(\n        new THREE.CylinderGeometry(0.004, 0.005, 0.045, 6),\n        matSkid\n      );\n      strut.position.set(fb, -0.042, skidZ);\n      // Slight outward splay\n      strut.rotation.z = side * 0.08;\n      quad.add(strut);\n    }\n\n    // Cross brace between struts\n    const brace = new THREE.Mesh(\n      new THREE.CylinderGeometry(0.003, 0.003, 0.12, 4),\n      matSkid\n    );\n    brace.rotation.z = Math.PI / 2;\n    brace.position.set(0, -0.048, skidZ);\n    quad.add(brace);\n\n    // Rubber feet at skid ends\n    for (const xOff of [-0.11, 0.11]) {\n      const foot = new THREE.Mesh(\n        new THREE.SphereGeometry(0.006, 6, 4),\n        new THREE.MeshStandardMaterial({ color: 0x111111, roughness: 0.9 })\n      );\n      foot.position.set(xOff, -0.068, skidZ);\n      foot.scale.y = 0.5;\n      quad.add(foot);\n    }\n  }\n\n  // \u2500\u2500 REAR STATUS LED BAR \u2500\u2500\n  const ledBar = new THREE.Mesh(\n    new THREE.BoxGeometry(0.06, 0.006, 0.004),\n    new THREE.MeshStandardMaterial({\n      color: 0x00aaff, emissive: 0x0066ff, emissiveIntensity: 1.5\n    })\n  );\n  ledBar.position.set(0, 0.01, -0.09);\n  quad.add(ledBar);\n\n  // \u2500\u2500 FRONT HEADLIGHT \u2500\u2500\n  const headlight = new THREE.Mesh(\n    new THREE.SphereGeometry(0.004, 6, 4),\n    new THREE.MeshStandardMaterial({\n      color: 0xffffee, emissive: 0xffffcc, emissiveIntensity: 3.0\n    })\n  );\n  headlight.position.set(0, -0.01, 0.092);\n  quad.add(headlight);\n  const headlightBeam = new THREE.SpotLight(0xffffdd, 0.4, 2, 0.5, 0.8);\n  headlightBeam.position.set(0, -0.01, 0.092);\n  headlightBeam.target.position.set(0, -0.5, 0.5);\n  quad.add(headlightBeam);\n  quad.add(headlightBeam.target);\n\n  state.scene.add(quad);\n  state.quad = quad;\n\n  // \u2550\u2550\u2550 FLIGHT TRAIL \u2550\u2550\u2550\n  const trailGeo = new THREE.BufferGeometry();\n  const trailPos = new Float32Array(TRAIL_MAX * 3);\n  trailGeo.setAttribute(\"position\", new THREE.BufferAttribute(trailPos, 3));\n  trailGeo.setDrawRange(0, 0);\n  const trail = new THREE.Line(trailGeo,\n    new THREE.LineBasicMaterial({ color: 0x1155cc, transparent: true, opacity: 0.6 })\n  );\n  state.scene.add(trail);\n  state.trail = trail;\n  state.trailCount = 0;\n  state.trailPos = trailPos;\n\n  // \u2550\u2550\u2550 GROUND SHADOW \u2550\u2550\u2550\n  const shadow = new THREE.Mesh(\n    new THREE.CircleGeometry(0.2, 20),\n    new THREE.MeshBasicMaterial({ color: 0x3a2a10, transparent: true, opacity: 0.3 })\n  );\n  shadow.rotation.x = -Math.PI / 2;\n  shadow.position.y = 0.003;\n  state.scene.add(shadow);\n  state.shadow = shadow;\n\n  state.frameCount = 0;\n};\n\n// \u2500\u2500 PROPELLER BLADE BUILDER \u2500\u2500\n// Creates a single blade with airfoil-like taper and twist\nfunction createPropBlade(THREE, cw) {\n  const blade = new THREE.Group();\n  const bladeLen = 0.11;\n  const segments = 6;\n\n  for (let i = 0; i < segments; i++) {\n    const t = (i + 0.5) / segments; // 0..1 along blade span\n    const segLen = bladeLen / segments;\n\n    // Chord tapers from root to tip\n    const chord = 0.022 * (1.0 - 0.5 * t);\n    // Thickness tapers\n    const thickness = 0.003 * (1.0 - 0.6 * t);\n\n    const seg = new THREE.Mesh(\n      new THREE.BoxGeometry(segLen, thickness, chord),\n      new THREE.MeshStandardMaterial({\n        color: 0x1a1a1a, metalness: 0.15, roughness: 0.55, side: THREE.DoubleSide\n      })\n    );\n\n    // Position along span\n    const r = 0.012 + bladeLen * t;\n    seg.position.x = (cw ? 1 : -1) * r;\n\n    // Twist: more pitch at root, less at tip (washout)\n    const twist = (cw ? 1 : -1) * (0.35 - 0.25 * t);\n    seg.rotation.x = twist;\n\n    // Slight droop at tips\n    seg.position.y = -t * t * 0.005;\n\n    blade.add(seg);\n  }\n\n  // Blade tip cap (rounded)\n  const tipCap = new THREE.Mesh(\n    new THREE.SphereGeometry(0.006, 6, 4),\n    new THREE.MeshStandardMaterial({ color: 0x1a1a1a, roughness: 0.5 })\n  );\n  tipCap.scale.set(0.5, 0.3, 1.0);\n  tipCap.position.x = (cw ? 1 : -1) * (0.012 + bladeLen);\n  blade.add(tipCap);\n\n  return blade;\n}\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n//  FRAME UPDATE\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nctx.onFrame = (api) => {\n  const { state } = api;\n  if (!state || !state.quad) return;\n  const si = api.sampleIndex;\n\n  // Read NED position\n  const px = Number(api.getValue(\"px\", si));\n  const py = Number(api.getValue(\"py\", si));\n  const pz = Number(api.getValue(\"pz\", si));\n\n  // NED \u2192 THREE.js (Y-up): East\u2192X, Up\u2192Y(-Down), North\u2192Z\n  const tx = Number.isFinite(py) ? py : 0;\n  const ty = Number.isFinite(pz) ? -pz : 0;\n  const tz = Number.isFinite(px) ? px : 0;\n\n  state.quad.position.set(tx, ty, tz);\n\n  // Quaternion attitude (body-to-world, NED)\n  const q0 = Number(api.getValue(\"q0\", si));\n  const q1 = Number(api.getValue(\"q1\", si));\n  const q2 = Number(api.getValue(\"q2\", si));\n  const q3 = Number(api.getValue(\"q3\", si));\n\n  if (Number.isFinite(q0) && Number.isFinite(q1) && Number.isFinite(q2) && Number.isFinite(q3)) {\n    // NED quat \u2192 THREE.js Y-up frame: R_THREE = M\u00b7R_NED\u00b7M\u1d40\n    // M maps NED(N,E,D)\u2192THREE(E,-D,N), det(M)=-1 flips all rotation senses\n    // Roll(q1)\u2192-z, Pitch(q2)\u2192-x, Yaw(q3)\u2192-y, Scalar(q0)\u2192w\n    const quat = new api.THREE.Quaternion(-q2, -q3, -q1, q0);\n    quat.normalize();\n    state.quad.setRotationFromQuaternion(quat);\n  }\n\n  // Spin propellers proportional to thrust\n  const T = Number(api.getValue(\"T\", si));\n  const spin = Number.isFinite(T) ? Math.max(T, 0) * 0.6 : 4.0;\n  state.frameCount = (state.frameCount || 0) + 1;\n\n  if (state.propGroups) {\n    state.propGroups.forEach((p) => {\n      const dir = p.cw ? 1 : -1;\n      p.group.rotation.y += dir * (0.3 + spin * 0.04);\n    });\n  }\n\n  // Trail\n  if (Number.isFinite(tx) && Number.isFinite(ty)) {\n    const idx = state.trailCount % TRAIL_MAX;\n    state.trailPos[idx * 3] = tx;\n    state.trailPos[idx * 3 + 1] = ty;\n    state.trailPos[idx * 3 + 2] = tz;\n    state.trailCount++;\n    state.trail.geometry.attributes.position.needsUpdate = true;\n    state.trail.geometry.setDrawRange(0, Math.min(state.trailCount, TRAIL_MAX));\n  }\n\n  // Ground shadow\n  if (state.shadow) {\n    state.shadow.position.set(tx, 0.003, tz);\n    const alt = Math.max(ty, 0.01);\n    const s = Math.max(0.4, 1.2 - alt * 0.04);\n    state.shadow.scale.set(s, s, 1);\n    state.shadow.material.opacity = Math.max(0.03, 0.25 - alt * 0.015);\n  }\n};\n";
