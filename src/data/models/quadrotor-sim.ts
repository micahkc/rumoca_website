export const QUADROTOR_SIM_SOURCE = "// =====================================================================\n// 6-DOF Rigid Body Kinematics \u2014 quaternion representation\n//\n// States: position p (world/NED), velocity v (world/NED),\n//         quaternion q (body-to-world), angular velocity omega (body)\n//\n// Quaternion convention: q = [q0, q1, q2, q3] = [w, x, y, z]\n//   q0 is the scalar part, q1..q3 are the vector part.\n//   Rotation from body to world: v_world = q \u2297 v_body \u2297 q*\n//\n// Kinematics:\n//   p_dot = v\n//   v_dot = R(q) * a_body + g_vec\n//   q_dot = 0.5 * q \u2297 [0, omega]\n// =====================================================================\n\nclass RigidBodyQuat\n\n  parameter Real g = 9.80665 \"Gravity [m/s^2]\";\n\n  // Position in world frame (NED)\n  Real px(start = 0) \"Position North [m]\";\n  Real py(start = 0) \"Position East [m]\";\n  Real pz(start = 0) \"Position Down [m]\";\n\n  // Velocity in world frame (NED)\n  Real vx(start = 0) \"Velocity North [m/s]\";\n  Real vy(start = 0) \"Velocity East [m/s]\";\n  Real vz(start = 0) \"Velocity Down [m/s]\";\n\n  // Unit quaternion (body-to-world), initialized to identity\n  Real q0(start = 1) \"Quaternion scalar (w)\";\n  Real q1(start = 0) \"Quaternion vector x\";\n  Real q2(start = 0) \"Quaternion vector y\";\n  Real q3(start = 0) \"Quaternion vector z\";\n\n  // Body-frame angular velocity\n  Real omega_x \"Body roll rate  [rad/s]\";\n  Real omega_y \"Body pitch rate [rad/s]\";\n  Real omega_z \"Body yaw rate   [rad/s]\";\n\n  // Body-frame specific force (excluding gravity)\n  Real a_bx \"Body specific force X [m/s^2]\";\n  Real a_by \"Body specific force Y [m/s^2]\";\n  Real a_bz \"Body specific force Z [m/s^2]\";\n\nequation\n\n  // --- Translational kinematics: p_dot = v ---\n  der(px) = vx;\n  der(py) = vy;\n  der(pz) = vz;\n\n  // --- Translational dynamics: v_dot = R(q)*a_body + g_vec ---\n  der(vx) = (1 - 2*(q2*q2 + q3*q3)) * a_bx\n          + 2*(q1*q2 - q0*q3)       * a_by\n          + 2*(q1*q3 + q0*q2)       * a_bz;\n\n  der(vy) = 2*(q1*q2 + q0*q3)       * a_bx\n          + (1 - 2*(q1*q1 + q3*q3)) * a_by\n          + 2*(q2*q3 - q0*q1)       * a_bz;\n\n  der(vz) = 2*(q1*q3 - q0*q2)       * a_bx\n          + 2*(q2*q3 + q0*q1)       * a_by\n          + (1 - 2*(q1*q1 + q2*q2)) * a_bz\n          + g;\n\n  // --- Quaternion kinematics: q_dot = 0.5 * q \u2297 [0, omega] ---\n  der(q0) = 0.5 * (-q1*omega_x - q2*omega_y - q3*omega_z);\n  der(q1) = 0.5 * ( q0*omega_x - q3*omega_y + q2*omega_z);\n  der(q2) = 0.5 * ( q3*omega_x + q0*omega_y - q1*omega_z);\n  der(q3) = 0.5 * (-q2*omega_x + q1*omega_y + q0*omega_z);\n\nend RigidBodyQuat;\n\n\n// =====================================================================\n// Quadrotor plant \u2014 extends RigidBodyQuat with angular dynamics\n// =====================================================================\n\nmodel QuadrotorQuat\n\n  extends RigidBodyQuat;\n\n  parameter Real mass = 2.0   \"Total mass [kg]\";\n  parameter Real Ixx  = 0.020 \"Roll  moment of inertia [kg*m^2]\";\n  parameter Real Iyy  = 0.020 \"Pitch moment of inertia [kg*m^2]\";\n  parameter Real Izz  = 0.040 \"Yaw   moment of inertia [kg*m^2]\";\n\n  // Control inputs (to be set by controller)\n  Real T  \"Total thrust [N]\";\n  Real Mx \"Roll  moment [N*m]\";\n  Real My \"Pitch moment [N*m]\";\n  Real Mz \"Yaw   moment [N*m]\";\n\nequation\n\n  // Thrust along body -Z \u2192 specific force\n  a_bx = 0;\n  a_by = 0;\n  a_bz = -T / mass;\n\n  // Euler's equations (diagonal inertia)\n  der(omega_x) = (Mx + (Iyy - Izz) * omega_y * omega_z) / Ixx;\n  der(omega_y) = (My + (Izz - Ixx) * omega_x * omega_z) / Iyy;\n  der(omega_z) = (Mz + (Ixx - Iyy) * omega_x * omega_y) / Izz;\n\nend QuadrotorQuat;\n\n\n// =====================================================================\n// Autopilot \u2014 velocity-command mode\n//\n// RC inputs (normalized -1..+1):\n//   rc_throttle  \u2192 vertical velocity   (+ = climb)\n//   rc_pitch     \u2192 forward velocity    (+ = forward)\n//   rc_roll      \u2192 rightward velocity  (+ = right)\n//   rc_yaw       \u2192 yaw rate            (+ = CW)\n//\n// Control architecture (lean \u2014 Euler angle attitude loop):\n//   1. RC stick \u2192 velocity setpoint (world frame, rotated by yaw)\n//   2. Velocity PI \u2192 desired world-frame acceleration\n//   3. Desired accel \u2192 desired roll/pitch (flatness) + thrust\n//   4. Euler angle P \u2192 desired body rates\n//   5. Rate PD \u2192 moments\n//\n// States (16): 13 plant + 3 velocity integrators\n// Algebraic: ~20 (vs ~60 in the quaternion-error version)\n// =====================================================================\n\nmodel Autopilot\n\n  extends QuadrotorQuat;\n\n  // RC input scaling\n  parameter Real max_vxy   = 5.0  \"Max horizontal velocity [m/s]\";\n  parameter Real max_vz    = 3.0  \"Max vertical velocity [m/s]\";\n  parameter Real max_yaw_rate = 1.5 \"Max yaw rate [rad/s]\";\n\n  // Velocity PI gains (conservative to avoid windup)\n  parameter Real Kp_vxy = 1.0 \"Horizontal velocity P gain\";\n  parameter Real Ki_vxy = 0.15 \"Horizontal velocity I gain\";\n  parameter Real Kp_vz  = 1.5 \"Vertical velocity P gain\";\n  parameter Real Ki_vz  = 0.3 \"Vertical velocity I gain\";\n\n  // Max desired tilt angle [rad] (~15 deg)\n  parameter Real max_tilt = 0.26;\n\n  // Attitude P gains (angle \u2192 rate)\n  parameter Real Kp_roll  = 6.0;\n  parameter Real Kp_pitch = 6.0;\n  parameter Real Kp_yaw   = 3.0;\n\n  // Rate PD gains (rate error \u2192 moment)\n  parameter Real Kd_roll  = 0.4;\n  parameter Real Kd_pitch = 0.4;\n  parameter Real Kd_yaw   = 0.6;\n\n  // RC inputs (bound by simulation model)\n  Real rc_throttle;\n  Real rc_pitch;\n  Real rc_roll;\n  Real rc_yaw;\n\n  // Velocity integrator states\n  Real vel_err_ix(start = 0);\n  Real vel_err_iy(start = 0);\n  Real vel_err_iz(start = 0);\n\n  // Euler angles extracted from quaternion\n  Real roll  \"Roll angle [rad]\";\n  Real pitch \"Pitch angle [rad]\";\n  Real yaw   \"Yaw angle [rad]\";\n\n  // Desired acceleration (world frame)\n  Real a_des_x;\n  Real a_des_y;\n  Real a_des_z;\n\n  // Desired Euler angles from flatness (unclamped + clamped)\n  Real roll_des_raw;\n  Real pitch_des_raw;\n  Real roll_des;\n  Real pitch_des;\n\n  // Desired body rates\n  Real p_des \"Desired roll rate\";\n  Real q_des \"Desired pitch rate\";\n  Real r_des \"Desired yaw rate\";\n\nequation\n\n  // =================================================================\n  // 1. Extract Euler angles from quaternion\n  // =================================================================\n  roll  = atan2(2*(q0*q1 + q2*q3), 1 - 2*(q1*q1 + q2*q2));\n  pitch = asin(2*(q0*q2 - q3*q1));\n  yaw   = atan2(2*(q0*q3 + q1*q2), 1 - 2*(q2*q2 + q3*q3));\n\n  // =================================================================\n  // 2. Velocity PI \u2192 desired world-frame acceleration\n  //    RC sticks are body-heading-relative, rotated to world by yaw.\n  //    Integrators clamped to [-5, 5] via conditional integration.\n  // =================================================================\n  der(vel_err_ix) = if vel_err_ix > 5 and (vx - max_vxy * (rc_pitch * cos(yaw) - rc_roll * sin(yaw))) > 0 then 0\n                    else (if vel_err_ix < -5 and (vx - max_vxy * (rc_pitch * cos(yaw) - rc_roll * sin(yaw))) < 0 then 0\n                    else vx - max_vxy * (rc_pitch * cos(yaw) - rc_roll * sin(yaw)));\n  der(vel_err_iy) = if vel_err_iy > 5 and (vy - max_vxy * (rc_pitch * sin(yaw) + rc_roll * cos(yaw))) > 0 then 0\n                    else (if vel_err_iy < -5 and (vy - max_vxy * (rc_pitch * sin(yaw) + rc_roll * cos(yaw))) < 0 then 0\n                    else vy - max_vxy * (rc_pitch * sin(yaw) + rc_roll * cos(yaw)));\n  der(vel_err_iz) = if vel_err_iz > 5 and (vz + max_vz * rc_throttle) > 0 then 0\n                    else (if vel_err_iz < -5 and (vz + max_vz * rc_throttle) < 0 then 0\n                    else vz + max_vz * rc_throttle);\n\n  a_des_x = -Kp_vxy * (vx - max_vxy * (rc_pitch * cos(yaw) - rc_roll * sin(yaw)))\n            - Ki_vxy * vel_err_ix;\n  a_des_y = -Kp_vxy * (vy - max_vxy * (rc_pitch * sin(yaw) + rc_roll * cos(yaw)))\n            - Ki_vxy * vel_err_iy;\n  a_des_z = -Kp_vz  * (vz + max_vz * rc_throttle)\n            - Ki_vz  * vel_err_iz;\n\n  // =================================================================\n  // 3. Desired accel \u2192 thrust + desired roll/pitch (with tilt clamp)\n  // =================================================================\n  T = mass * sqrt(a_des_x * a_des_x\n                + a_des_y * a_des_y\n                + (a_des_z - g) * (a_des_z - g) + 1e-8);\n\n  // Flatness inversion (NED): to accelerate +x (North), pitch nose down (negative pitch)\n  // roll_des  =  (sin(yaw)*ax - cos(yaw)*ay) / g  \u2192 positive roll = tilt right\n  // pitch_des = -(cos(yaw)*ax + sin(yaw)*ay) / g  \u2192 negative sign for NED pitch convention\n  roll_des_raw  = ( sin(yaw) * a_des_x - cos(yaw) * a_des_y) / g;\n  pitch_des_raw = -( cos(yaw) * a_des_x + sin(yaw) * a_des_y) / g;\n\n  // Clamp desired angles to max_tilt\n  roll_des  = if roll_des_raw > max_tilt then max_tilt\n              else (if roll_des_raw < -max_tilt then -max_tilt\n              else roll_des_raw);\n  pitch_des = if pitch_des_raw > max_tilt then max_tilt\n              else (if pitch_des_raw < -max_tilt then -max_tilt\n              else pitch_des_raw);\n\n  // =================================================================\n  // 4. Attitude P \u2192 desired body rates\n  // =================================================================\n  p_des = Kp_roll  * (roll_des  - roll);\n  q_des = Kp_pitch * (pitch_des - pitch);\n  r_des = rc_yaw * max_yaw_rate;\n\n  // =================================================================\n  // 5. Rate PD \u2192 moments\n  // =================================================================\n  Mx = Ixx * (p_des - (1 + Kd_roll)  * omega_x) + (Iyy - Izz) * omega_y * omega_z;\n  My = Iyy * (q_des - (1 + Kd_pitch) * omega_y) + (Izz - Ixx) * omega_x * omega_z;\n  Mz = Izz * (r_des - (1 + Kd_yaw)   * omega_z) + (Ixx - Iyy) * omega_x * omega_y;\n\nend Autopilot;\n\n// =====================================================================\n// Autopilot simulation \u2014 fly a demo mission with time-varying RC inputs\n//\n// Uses linear ramps (0.5s) instead of step inputs to avoid solver issues.\n//\n// Flight profile (NED frame, pz negative = altitude):\n//   0 -  3s : Climb to altitude\n//   3 -  5s : Hover / stabilize\n//   5 -  8s : Fly forward\n//   8 - 10s : Hover\n//  10 - 13s : Fly right\n//  13 - 15s : Hover\n//  15 - 18s : Fly backward\n//  18 - 20s : Hover\n//  20 - 23s : Fly left\n//  23 - 25s : Hover\n//  25 - 28s : Yaw CW\n//  28 - 30s : Hover\n//  30 - 33s : Diagonal forward-right\n//  33 - 35s : Hover\n//  35 - 38s : Descend\n//  38 - 40s : Idle\n// =====================================================================\n\nmodel AutopilotSim\n\n  extends Autopilot;\n\n  // Linear ramp helper variables (ramp up over 0.5s, hold, ramp down over 0.5s)\n  Real ramp_throttle1 \"Climb ramp\";\n  Real ramp_fwd       \"Forward ramp\";\n  Real ramp_right     \"Right ramp\";\n  Real ramp_back      \"Backward ramp\";\n  Real ramp_left      \"Left ramp\";\n  Real ramp_yaw       \"Yaw ramp\";\n  Real ramp_diag      \"Diagonal ramp\";\n  Real ramp_desc      \"Descend ramp\";\n\nequation\n\n  // === Linear ramp signals: 0\u21921 over 0.5s, hold, 1\u21920 over 0.5s ===\n\n  // Climb: ramp up at 0s, ramp down at 2.5s\n  ramp_throttle1 = (if time < 0.5 then time / 0.5\n                    else (if time < 2.5 then 1\n                    else (if time < 3.0 then (3.0 - time) / 0.5\n                    else 0)));\n\n  // Forward: 5-8s\n  ramp_fwd = (if time < 5.0 then 0\n              else (if time < 5.5 then (time - 5.0) / 0.5\n              else (if time < 7.5 then 1\n              else (if time < 8.0 then (8.0 - time) / 0.5\n              else 0))));\n\n  // Right: 10-13s\n  ramp_right = (if time < 10.0 then 0\n                else (if time < 10.5 then (time - 10.0) / 0.5\n                else (if time < 12.5 then 1\n                else (if time < 13.0 then (13.0 - time) / 0.5\n                else 0))));\n\n  // Backward: 15-18s\n  ramp_back = (if time < 15.0 then 0\n               else (if time < 15.5 then (time - 15.0) / 0.5\n               else (if time < 17.5 then 1\n               else (if time < 18.0 then (18.0 - time) / 0.5\n               else 0))));\n\n  // Left: 20-23s\n  ramp_left = (if time < 20.0 then 0\n               else (if time < 20.5 then (time - 20.0) / 0.5\n               else (if time < 22.5 then 1\n               else (if time < 23.0 then (23.0 - time) / 0.5\n               else 0))));\n\n  // Yaw CW: 25-28s\n  ramp_yaw = (if time < 25.0 then 0\n              else (if time < 25.5 then (time - 25.0) / 0.5\n              else (if time < 27.5 then 1\n              else (if time < 28.0 then (28.0 - time) / 0.5\n              else 0))));\n\n  // Diagonal: 30-33s\n  ramp_diag = (if time < 30.0 then 0\n               else (if time < 30.5 then (time - 30.0) / 0.5\n               else (if time < 32.5 then 1\n               else (if time < 33.0 then (33.0 - time) / 0.5\n               else 0))));\n\n  // Descend: 35-38s\n  ramp_desc = (if time < 35.0 then 0\n               else (if time < 35.5 then (time - 35.0) / 0.5\n               else (if time < 37.5 then 1\n               else (if time < 38.0 then (38.0 - time) / 0.5\n               else 0))));\n\n  // === RC stick inputs (moderate amplitudes) ===\n  rc_throttle = 0.6 * ramp_throttle1 - 0.4 * ramp_desc;\n  rc_pitch    = 0.3 * ramp_fwd - 0.3 * ramp_back + 0.2 * ramp_diag;\n  rc_roll     = 0.3 * ramp_right - 0.3 * ramp_left + 0.2 * ramp_diag;\n  rc_yaw      = 0.3 * ramp_yaw;\n\nend AutopilotSim;\n";
